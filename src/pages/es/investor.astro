---
import Layout from "@/layouts/Layout.astro";
import Menu from "@/components/Menu.astro";
import SellerForm from "@/components/SellerForm.tsx";
import ResponseForm from "@/components/ResponseForm.astro";
import Faqs from "@/components/Faqs.astro";

import { useTranslations } from "@/i18n/utils";
import { languages } from "@/i18n/ui";

const relativeLocale = Astro.currentLocale || "en";
const t = useTranslations(relativeLocale as keyof typeof languages);

import type { LabelsSellerParams } from "@/utils/property";
import Picture from "astro/components/Picture.astro";
import localImage from "@/../public/images/bg/bg-investor.webp";
import investorImage from "@/../public/images/investor.webp";

const labels = {
  title: t("investor.form.title"),
  description: t("investor.form.description"),
  firstname: t("investor.form.first.name"),
  lastname: t("investor.form.last.name"),
  email: t("investor.form.email"),
  prefix: t("investor.form.prefix"),
  phone: t("investor.form.phone"),
  location: t("investor.form.location"),
  meters: t("investor.form.square.meters"),
  bedsbaths: t("investor.form.bedrooms.bathrooms"),
  bedrooms: t("investor.form.bedrooms"),
  bathrooms: t("investor.form.bathrooms"),
  items: t("investor.form.selec.items"),
  parking: t("investor.form.parking"),
  pool: t("investor.form.pool"),
  view: t("investor.form.view"),
  button: t("investor.form.botton"),
  buttontext: t("investor.form.text"),
} as LabelsSellerParams;

const title = t("SEO.investor.title");
const description = t("SEO.investor.description");
---

<Layout title={title} description={description}>
  <Menu isFloating isWhite />
  <div class="h-[276px] w-full flex justify-center items-center">
    <Picture
      src={localImage}
      formats={["avif", "webp", "jpeg"]}
      alt="background home image"
      style={{
        objectFit: "cover",
        objectPosition: "bottom",
        width: "100%",
        height: "100%",
      }}
      pictureAttributes={{
        class: "w-full h-full",
      }}
    />
  </div>

  <section class="max-w-7xl mx-auto mb-10 mt-20 px-10">
    <div
      class="flex flex-col lg:flex-row justify-between items-center my-10 mx-auto gap-10 lg:gap-20"
    >
      <h2
        class="w-full text-center lg:text-left lg:w-[500px] uppercase font-serif text-3xl md:text-4xl"
      >
        {t("investor.title1")}
        <div class="italic normal-case mt-2">
          {t("investor.title2")}
        </div>
      </h2>
      <p class="w-full text-center lg:text-left lg:w-1/2 text-pretty">
        {t("investor.description")}
      </p>
    </div>

    <div class="flex justify-between items-start gap-20 mt-20 mb-48">
      <Picture
        src={investorImage}
        formats={["avif", "webp", "jpeg"]}
        alt="background home image"
        style={{
          borderRadius: "1rem",
        }}
        pictureAttributes={{
          class: "hidden lg:block w-[500px] xl:w-fit",
        }}
      />

      <SellerForm labels={labels} type="investor" client:load />
    </div>
  </section>

  <Faqs type="investor" />
</Layout>

<ResponseForm
  msgSuccess="Thank you for your message! We will contact you soon."
  msgError="We are experiencing problems at this time. Please try again later."
/>
